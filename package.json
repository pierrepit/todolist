{
	"name": "todolist",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"devstart": "concurrently \"cd server && npm run devstart\" \"cd front && npm start\" ",
		"start": "node --experimental-modules server/index.mjs",
		"build": "cd server && npm install && npm start  && cd ../front && npm install && npm start && npm run build"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/pierrepit/todolist.git"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/pierrepit/todolist/issues"
	},
	"homepage": "https://github.com/pierrepit/todolist#readme",
	"devDependencies": {
		"concurrently": "^7.6.0"
	}
}
